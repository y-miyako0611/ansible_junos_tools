---
- name: "aws でshowコマンドのテスト"
  hosts: "{{ vsrx_host }}"
  gather_facts: false
  tasks:
    - name: "{{ private_cmd_1 }} の確認"
      junos_command:
        commands: "{{ private_cmd_1 }}"
      register: results

    - name: "{{ private_cmd_1 }}  結果表示"
      debug:
        msg: "{{ results.stdout_lines[0] }}"

    - name: "{{ private_cmd_2 }} の確認"
      junos_command:
        commands: "{{ private_cmd_2 }}"
      register: results2

    - name: "{{ private_cmd_2 }} 結果表示"
      debug:
        msg: "{{ results2.stdout_lines[0] }}"

    - name: "{{ private_cmd_3 }} の確認"
      junos_command:
        commands: "{{ private_cmd_3 }}"
      register: results3

    - name: "show chassis hardware 結果表示"
      debug:
        msg: "{{ results3.stdout_lines[0] }}"
